sudo apt update
sudo apt install wget maven python3.12 rabbitmq-server audacity portaudio19-dev flac
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install nodejs -y
sudo systemctl enable rabbitmq-server
sudo systemctl start rabbitmq-server
wget https://download.oracle.com/java/21/latest/jdk-21_linux-aarch64_bin.tar.gz
tar -xvf jdk-21_linux-aarch64_bin.tar.gz
cd jdk-21.0.1
sudo mkdir -p /usr/local/jdk-21
sudo mv * /usr/local/jdk-21
cd ..
cd /usr/lib/python3.12
sudo rm EXTERNALLY-MANAGED
cd ~
wget https://download.jetbrains.com/idea/ideaIU-2023.2.5.tar.gz
tar -xvf ideaIU-2023.2.5.tar.gz
source ~/.bashrc
echo 'export JAVA_HOME=/usr/local/jdk-21' >> ~/.bashrc
echo 'export PATH=$JAVA_HOME/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
sudo apt update
sudo apt upgrade
pip3 install adafruit-blinka
pip3 install adafruit-circuitpython-servokit
pip3 install adafruit_pca9685
pip3 install pika
pip3 install RPi.GPIO
pip3 install sounddevice
pip3 install numpy
pip3 install scipy
pip3 install soundfile
pip3 install setuptools
pip3 install SpeechRecognition
pip3 install pyaudio
pip3 install SpeechRecognition[whisper-local]
pip3 install piper-tts
pip3 install invoke
pip3 install fabric
pip3 install librosa
pip3 install pydub

cd ~/Desktop/
mkdir piper
cd piper
wget https://github.com/rhasspy/piper/releases/download/v1.2.0/piper_arm64.tar.gz
wget https://huggingface.co/rhasspy/piper-voices/resolve/v1.0.0/nl/nl_NL/mls/medium/nl_NL-mls-medium.onnx?download=true
cd ..
mkdir vosk
cd vosk
wget https://alphacephei.com/vosk/models/vosk-model-small-nl-0.22.zip
wget https://alphacephei.com/vosk/models/vosk-model-nl-spraakherkenning-0.6-lgraph.zip
cd ..

--------------------------------------------------------------------------------------------------------------

sudo raspi-config
Interface options -> i2c -> Enable -> Yes -> Finish
Interface options -> ssh -> Enable -> Yes -> Finish
